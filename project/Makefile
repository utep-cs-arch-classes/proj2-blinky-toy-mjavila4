all:
	(cd timerLib && make install)
	(cd toy && make)
	(cd button_proj && make)
	(cd buzzer_proj && make)

clean:
	(cd timerLib && make clean)
	(cd toy && make clean)
	(cd button_proj && make clean)
	(cd buzzer_proj && make clean)

